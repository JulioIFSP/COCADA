import{BadRequestException,Injectable}from'@nestjs/common';import{StorageService}from'../storage/storage.service';import{CreateClinicaDto,UpdateClinicaDto}from'./dto/clinica.dto';type C=CreateClinicaDto&{id:string};@Injectable()export class ClinicasService{constructor(private s:StorageService){}list(){return this.s.list<C>('clinicas')}get(id:string){return this.s.get<C>('clinicas',id)}create(d:CreateClinicaDto){return this.s.create('clinicas',d)}update(id:string,d:UpdateClinicaDto){return this.s.update<C>('clinicas',id,d)}remove(id:string){if(this.s.list<{clinicaId:string}>('medicos').some(x=>x.clinicaId===id))throw new BadRequestException('Não é possível excluir clínica com médicos.');this.s.remove<C>('clinicas',id)}}
